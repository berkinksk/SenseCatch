# Serving stack for the container. The CPU build of torch is installed
# separately in the Dockerfile, so it is not listed here.
flask==3.1.3
werkzeug==3.1.8
jinja2==3.1.6
itsdangerous==2.2.0
click==8.3.1
blinker==1.9.0
gunicorn==23.0.0
flask-cors==6.0.2
scikit-learn==1.8.0
numpy==2.4.2
scipy==1.17.1
nltk==3.7
transformers==5.2.0
# Imported transitively when the stacked ensemble option loads its base models.
datasets==5.0.0
